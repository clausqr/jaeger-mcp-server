# Jaeger API URL (required). HTTP or gRPC base URL, e.g. http://localhost:16686 or http://localhost:16685
JAEGER_URL=

# Protocol: GRPC (default) or HTTP. Set HTTP when using the Jaeger UI/HTTP API port (e.g. 16686).
# JAEGER_PROTOCOL=HTTP

# Request timeout in ms for API calls (e.g. find-traces). Default 60000. Omit for default.
# JAEGER_REQUEST_TIMEOUT_MS=30000

# Set to 1 to enable server debug logs (e.g. find-traces start/end).
# JAEGER_DEBUG=1
